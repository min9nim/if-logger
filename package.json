{
  "name": "if-logger",
  "version": "0.0.6",
  "description": "`if-logger` is a logger that can be set to log or not dynamically",
  "keywords": [
    "logger"
  ],
  "main": "dist/index.js",
  "author": "min9nim@gmail.com",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json --declaration --outDir dist",
    "test": "mocha -r ts-node/register test/**/*.spec.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/min9nim/if-logger.git"
  },
  "devDependencies": {
    "@types/es6-shim": "^0.31.39",
    "@types/mocha": "^5.2.7",
    "chai": "^4.2.0",
    "mocha": "^7.0.0",
    "sinon": "^8.1.1",
    "ts-node": "^8.5.4",
    "tslib": "^1.10.0",
    "typescript": "^3.7.4"
  },
  "dependencies": {}
}
